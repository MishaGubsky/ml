## Environment setup:

1. install miniconda for virtualenv (!important) (https://conda.io/miniconda.html)
2. create env `conda create -n ml python=3`
3. activate env (every time) `source activate ml`
4. install requirements via `pip install -r requirements.txt`

#### Start scripts

Start a lab script by running: `python {script_name}.py`
